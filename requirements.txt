ollama>=0.4.0
openai>=1.0.0
playwright>=1.40.0
httpx>=0.27.0
rich>=13.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
psutil>=5.9.0

# Phase 2 — Knowledge Engine (TurboVec replaces ChromaDB)
turbovec>=0.1.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
watchdog>=4.0.0
tree-sitter>=0.22.0
tree-sitter-python>=0.22.0
tree-sitter-javascript>=0.22.0
tree-sitter-typescript>=0.22.0
prompt-toolkit>=3.0.0

# LLM providers
google-genai>=1.0.0
python-dotenv>=1.0.0
# Model fleet config (config/models.yaml)
PyYAML>=6.0

# Offline voice (free — no cloud APIs)
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
pyttsx3>=2.90
# Windows SAPI — enables barge-in ("stop" mid-sentence); falls back to PowerShell TTS
pywin32>=306; sys_platform == "win32"

# Phase 2B — document parsers (real extractors, not fake text)
pymupdf>=1.24.0
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0

# Phase 2B — optional database drivers (tools report unavailable if missing)
psycopg2-binary>=2.9.9
pymongo>=4.6.0
