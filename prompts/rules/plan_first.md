# Plan first

- Name the files and success checks before editing.
- Do not start a second planner call after the first plan is accepted.
- Simple one-file edits may use the heuristic plan; skip a model call.
