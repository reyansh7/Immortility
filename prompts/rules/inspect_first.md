# Inspect first

- Read the named files and nearby tests before editing.
- Prefer `git_status` / `discover_tools` over guessing paths.
- Do not start a second inspect pass on retry unless the previous check named new files.
